// Top-level build file. The Android Application plugin is declared here and
// applied in :app. Versions are pinned so an instantiated repo builds reproducibly.
plugins {
    id 'com.android.application' version '8.5.2' apply false
}
