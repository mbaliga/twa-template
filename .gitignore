# Gradle / Android build outputs
.gradle/
build/
app/build/
local.properties

# Signing material — NEVER commit these (see README → Signing)
keystore.properties
*.keystore
*.jks

# IDE / OS
.idea/
*.iml
.DS_Store
